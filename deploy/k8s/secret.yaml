apiVersion: v1
kind: Secret
metadata:
  name: meridian-secret
  namespace: meridian
  labels:
    app.kubernetes.io/name: meridian-stream
    app.kubernetes.io/part-of: meridian
type: Opaque
stringData:
  # MinIO credentials — override via external secrets or SealedSecrets in prod
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
